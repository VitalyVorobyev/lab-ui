/** Register one test per story in `modules` that renders it and runs its `play`. */
export declare function runStories(modules: Record<string, unknown>, annotations?: Record<string, unknown>): void;
